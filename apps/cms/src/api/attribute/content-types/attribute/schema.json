{
  "kind": "collectionType",
  "collectionName": "attributes",
  "info": {
    "singularName": "attribute",
    "pluralName": "attributes",
    "displayName": "Attribute",
    "description": "Thuộc tính sản phẩm (màu men, kích thước, chất liệu...)"
  },
  "options": {
    "draftAndPublish": false
  },
  "pluginOptions": {
    "i18n": {
      "localized": true
    }
  },
  "attributes": {
    "name": {
      "type": "string",
      "required": true,
      "pluginOptions": { "i18n": { "localized": true } }
    },
    "slug": {
      "type": "uid",
      "targetField": "name",
      "required": true,
      "pluginOptions": { "i18n": { "localized": false } }
    },
    "type": {
      "type": "enumeration",
      "enum": ["text", "color", "select", "number"],
      "default": "text",
      "pluginOptions": { "i18n": { "localized": false } }
    },
    "options": {
      "type": "json",
      "pluginOptions": { "i18n": { "localized": true } }
    }
  }
}
